query getGoals($budgetId: UUID!) {
    goals(budgetId: $budgetId) {
        id
        name
        type
        description
        min
        max
        category {
            id
            name
        }
    }
}