mutation updateGoals($budgetId: UUID!, $goals: [GoalInput!]!) {
    updateGoals(budgetId: $budgetId, goals: $goals) {
        id
        name
        description
        type
        value
        category {
            id
            name
        }
    }
}