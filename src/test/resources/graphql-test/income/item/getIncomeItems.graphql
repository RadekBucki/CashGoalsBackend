query getIncomeItems($budgetId: UUID!, $month: Int!, $year: Int!) {
    incomeItems(budgetId: $budgetId, month: $month, year: $year) {
        id
        income {
            id
            name
            description
        }
        name
        description
        date
        amount
    }
}