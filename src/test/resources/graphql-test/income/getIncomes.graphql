query getIncomes($budgetId: ID!) {
    incomes(budgetId: $budgetId) {
        id
        name
        description
        amount
        frequency {
            period
            value
        }
    }
}