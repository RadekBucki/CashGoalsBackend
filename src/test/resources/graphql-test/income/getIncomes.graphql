query getIncomes($budgetId: UUID!) {
    incomes(budgetId: $budgetId) {
        id
        name
        description
        amount
        frequency {
            period
            value
        }
    }
}