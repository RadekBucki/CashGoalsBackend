query getCategories($budgetId: UUID!) {
    categories(budgetId: $budgetId) {
        ...CategoryDetails
    }
}
fragment CategoryDetails on Category {
    id
    name
    description
    visible
    children {
        ...CategoryDetails
    }
}