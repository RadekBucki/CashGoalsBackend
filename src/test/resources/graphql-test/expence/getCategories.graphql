query getCategories($budgetId: UUID!) {
    categories(budgetId: $budgetId) {
        id
        name
        description
        visible
        children {
            id
            name
            description
            visible
            children {
                id
                name
                description
                visible
            }
        }
    }
}