query getVisibleCategories($budgetId: UUID!) {
    visibleCategories(budgetId: $budgetId) {
        id
        name
        description
        visible
        children {
            id
            name
            description
            visible
        }
    }
}