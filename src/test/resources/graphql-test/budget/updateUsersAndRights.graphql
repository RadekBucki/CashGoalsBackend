mutation updateUsersAndRights($budgetId: ID!, $usersRights: [UserRightsInput!]!) {
    updateUsersRights(budgetId: $budgetId, usersRights: $usersRights) {
        user {
            name
            email
        }
        rights
    }
}