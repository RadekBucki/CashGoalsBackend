spring:
  graphql:
    schema:
      printer:
        enabled: true
    graphiql:
      enabled: true
  jpa:
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
debug: true
